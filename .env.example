# Public n8n webhook for the Voice Agent workflow (ElevenLabs + STT/TTS live in n8n only).
VITE_N8N_VOICE_WEBHOOK_URL=

# Optional: hosted greeting audio when the floating Jarvis opens (otherwise Web Speech API).
VITE_JARVIS_GREETING_AUDIO_URL=
